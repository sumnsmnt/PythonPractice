# Set is the collection of the unordered items
# Each element in the set must be unique and immutable

collection = {1, 2, 3, "hello", "hi", 4, 5, 3, 2, 1}

# duplicate elements prints only once and 
# while couting lenth of the set also it ignores duplicate elements.
print(collection)
print(type(collection))
print(len(collection))

# Empty Set
collection = set()
print(type(collection))